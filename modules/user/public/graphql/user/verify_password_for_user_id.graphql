query verify($id: ID!, $password: String!){
  users(
    filter: {
      id: { value: $id }
    }
    per_page: 1
  ){
    results{
      id
      email
      authenticate{
        password(password: $password)
      }
    }
  }
}
