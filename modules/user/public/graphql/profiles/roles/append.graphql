mutation (
  $id: ID!
  $role: String!
) {
  record: record_update(
    id: $id
    record: {
      table: "modules/user/profile"
      properties: [
        { name: "roles", array_append: $role }
      ]
    }
  ) {
    id
    created_at
    type: table

    properties
  }
}
