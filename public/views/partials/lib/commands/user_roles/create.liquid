{% comment %}
  Sets a user's roles.
  Params:
    - user_id: string
      the user's ID
    - roles: array of strings
      the roles that assigned to the user
{% endcomment %}
{% liquid
  graphql user_role = 'modules/user/user_roles/create', user_id: user_id, roles: roles
  return user_role
%}
