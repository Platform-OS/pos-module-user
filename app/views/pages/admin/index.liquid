{% liquid
  function current_user = 'modules/user/queries/user/current'
  # platformos-check-disable UnreachableCode
  include 'modules/user/helpers/can_do_or_unauthorized', requester: current_user, do: 'admin.read', redirect_anonymous_to_login: true
  # platformos-check-enable UnreachableCode

  function users = 'modules/user/queries/user/search', id: null, not_ids: null, email: null, role: null, roles: null, sort: null, limit: 20

  render 'admin/home/index', users: users
%}
